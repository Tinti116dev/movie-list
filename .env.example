# Copy this file to `.env` and replace the placeholder with your TMDB API key.
# For Vite, environment variables used in client code must be prefixed with VITE_
VITE_TMDB_API_KEY=your_tmdb_api_key_here
